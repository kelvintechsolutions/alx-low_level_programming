/**
 * op_add -it is the sum of a and b.
 * @a:it is the  first operand.
 * @b:it is the second operand.
 * Return:it will return (a + b).
 */
int op_add(int a, int b)
{
	return (a + b);
}
/**
 * op_sub -it will substract of a and b.
 * @a:it is the first operand.
 * @b:it is the second operand.
 * Return:it will return (a – b).
 */
int op_sub(int a, int b)
{
	return (a - b);
}
/**
 * op_mul -it will multiply of a and b.
 * @a:it is the  first operand.
 * @b:it is the second operand.
 * Return: it will retern(a * b).
 */
int op_mul(int a, int b)
{
	return (a * b);
}
/**
 * op_div -it will divide of a and b.
 * @a:it is the first operand.
 * @b:it is the  second operand.
 * Return:it will return (a / b).
 */
int op_div(int a, int b)
{
	return (a / b);
}
/**
 * op_mod -it will module of a and b.
 * @a:it is the first operand.
 * @b: it is the second operand.
 * Return:it will return( a % b)
 */
int op_mod(int a, int b)
{
	return (a % b);
}
