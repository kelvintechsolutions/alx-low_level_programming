#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main -it checks the code.
 *
 * Return:it  always returns EXIT_SUCCESS;
 */
int main(void)
{
printf("%d\n", _strlen("My Dyn lib"));
return (EXIT_SUCCESS);
}
