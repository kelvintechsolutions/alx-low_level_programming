#include "main.h"

/**
 * _islower -it prints 1 or 0 depending on input.
 * @c:it is the first parameter.
 * Description:it prints all lowercase letters.
 * Return:it returns always(0).
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
