#include <stdio.h>


/**
 *add- addition.
 *sub- subtraction.
 *mul- multiplication.
 *div- division.
 *mod-it calculates the modulus.
 *@a- it is the fisrt interger.
 *@b- it is the second interger.
 *return: it  will return 1 if success.
 */
int add(int a, int b);
int sub(int a, int b);
int mul(int a, int b);
int div(int a, int b);
int mod(int a, int b);

int add(int a, int b)
{
(return a + b);
}

int sub(int a, int b)
{
(return a - b);
}
int mul(int a, int b)
{
(return a * b);
}
int div(int a, int b)
{
(return a / b);
}
int mod(int a, int b)
{
(return a % b);
}
